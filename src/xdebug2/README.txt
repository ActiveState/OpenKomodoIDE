The xdebug/php-build support for Komodo's PHP debugging.

Usage:
  1. Ensure "mk" is on your PATH.
  2. Type "mk" in the "src/xdebug2" directory for make options.
  3. Tweak the "config.py" as desired (add/remove php versions, etc...)
  4. Run "mk xdebugs"
